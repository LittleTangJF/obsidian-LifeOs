[参考](https://www.yuque.com/cuggz/interview/evfmq3)

1.CSS选择器及其优先级
- 内敛样式 1000
- id： 100
- class：类 10
- 属性选择器 a[ref = 11] 10
- 伪类选择器 li:first-child 10
- 标签选择器 li div 1
- 伪元素 li:before  1
- 相邻兄弟选择器 0
- 子 0
- 后代 0
- 通配符 * 0

2.盒模型
box-sizing属性改变盒模型`box-sizing: content-box`表示标准盒模型，`box-sizing: border-box`表示IE盒模型
- 标准：width = content 盒子大小=width+padding+border+margin
- IE：width= content + panding+border 盒子大小= width+ margin
3.CSS3中有哪些新特性
- transform
- gradient
- border-radius

4.CSSSprites
将一个页面涉及到的所有图片都包含到一张大图中去，然后利用CSS的 background-image，background-repeat，background-position属性的组合进行背景定位。

5.Flexbox 高频问题
**主轴与交叉轴​**​：Flexbox布局的核心。主轴方向由 `flex-direction`决定（默认为 `row`，水平方向），交叉轴则与主轴垂直 理解这一点是使用 `justify-content`和 `align-items`的基础。
- ​**​`flex: 1`到底是什么**: `flex: 1`等价于 `flex: 1 1 0%`​**​该项目可以根据剩余空间自由伸缩（增长因子为1，收缩因子为1），且它在分配多余空间前的基准尺寸为0​**​`flex`是 `flex-grow`、`flex-shrink`和 `flex-basis`的简写
- `flex: auto`：等价于 `1 1 auto ---项目根据内容大小分配空间 默认
- `flex: none`：等价于 `0 0 auto ---项目不伸缩，保持原有尺寸
- `flex: 200px`：等价于 `1 1 200wpx ---基准为200px，然后按比例伸缩
6.**圣杯布局（经典三栏布局）**
```css
.container {
  display: flex;
  flex-direction: column; /* 设置为垂直方向为主轴 */
  min-height: 100vh; /* 容器至少为视口高度 */
}
.header, .footer { height: 60px; }
.main { flex: 1; } /* 关键：中间部分占据所有剩余空间 */

```

7.水平垂直居中的实现
- 绝对定位

```css
绝对定位
.parent {
    position: relative;
}
 
.child {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%,-50%);
}
**flexbox
.parent {
    display: flex;
    justify-content:center;
    align-items:center;
}
```
9.九宫格
```css
ul {
  width: 100%;
  height: 100%;
  display: grid;
  grid-template-columns: 30% 30% 30%; 
  grid-template-rows: 30% 30% 30%; 
  grid-gap: 5%; 
}
```

10.清除浮动
- **空标签法​**：在浮动元素末尾添加空标签（如 `<div>`）并设置 `clear: both;`
- 给父元素添加 `::after`伪元素，并设置 `display: block; clear: both; content: '';`语义性好，无需冗余标签，
- BFC元素在计算高度时，会包含其内部的浮动元素，常用触发方式：`overflow: hidden`或 `overflow: auto`
- 定高发：直接给父元素设置一个固定的高度（`height`）|

11.BFC
**块级格式化上下文**
BFC是一个独立的布局环境，可以理解为一个容器，在这个容器中按照一定规则进行物品摆放，并且不会影响其它环境中的物品。
- 创建
	- 元素设置float÷none
	- 元素设置绝对定位
	- display： inline-block flex table-cell
	- overflow: hiden auto scroll
- **BFC的特点**
	- 两个容器的margin会重叠
	- BFC是独立的容器，容器内部元素不会影响外部元素
	- BFC区域不会与浮动的容器发生重叠
- 作用
	- **解决margin的重叠问题**：由于BFC是一个独立的区域，内部的元素和外部的元素互不影响，将两个元素变为两个BFC，就解决了margin重叠的问题。
	- **解决高度塌陷的问题**：在对子元素设置浮动后，父元素会发生高度塌陷，也就是父元素的高度变为0。解决这个问题，只需要把父元素变成一个BFC。常用的办法是给父元素设置`overflow:hidden`
	- **创建自适应两栏布局**：可以用来创建自适应两栏布局：左边的宽度固定，右边的宽度自适应
```css
.left{
     width: 100px;
     height: 200px;
     background: red;
     float: left;
 }
 .right{
     height: 300px;
     background: blue;
     overflow: hidden;
 }
 
<div class="left"></div>
<div class="right"></div>
```

12.什么是margin重叠问题？如何解决？
**定义**：两个块级元素的上外边距和下外边距可能会合并（折叠）为一个外边距其大小会取其中外边距值大的那个，这种行为就是外边距折叠；重叠只会出现在**垂直方向**

- 兄弟之间重叠
	- 底部元素变为行内盒子：`display: inline-block`
- 父子之间重叠
	- 父元素加入：`overflow: hidden`
	- 子元素变为行内盒子：`display: inline-block`

13.元素的层叠顺序

- background border
- index-z负数
- 块级盒子
- 浮动盒子
- 行内盒子
- index-z=0
- index-z>0

14、**em 与 rem 区别**

- `em` 相对于 **父元素字体大小**
- `rem` 相对于 **根元素（html）的字体大小**