### js

一、**事件循环（Event Loop）**

- 宏任务（macrotask）：`setTimeout`, `setInterval`, `setImmediate`, `I/O`, `UI rendering`
- 微任务（microtask）：`Promise.then`, `process.nextTick`, `queueMicrotask`, `MutationObserver`
- 执行顺序：**先执行同步代码 → 清空所有微任务 → 执行下一个宏任务**
二、**闭包**

